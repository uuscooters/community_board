name: domain
description: "The domain layer shared across all apps"
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.12.0
  flutter: ">=1.17.0"

dependencies:
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  fpdart: ^1.2.0

  core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
